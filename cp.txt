
Q.checking 2 strings are anagrams are not.

var firstWord = "ram";
var secondWord = "mar";



function isAnagram(firstWord, secondWord) {
  
  var a = first.toLowerCase();
  var b = second.toLowerCase();

  
  a = a.split("").sort().join("");
  b = b.split("").sort().join("");

  return a === b;
}